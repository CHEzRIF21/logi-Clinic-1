import React from 'react';
import { Box, Typography, Alert } from '@mui/material';

export const ParametresDiagnosticsFavoris: React.FC = () => {
  return (
    <Box>
      <Alert severity="info">
        Cette section permettra de gérer les diagnostics favoris par utilisateur.
        (À implémenter)
      </Alert>
    </Box>
  );
};

