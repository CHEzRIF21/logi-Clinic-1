import React from 'react';
import { Box, Typography, Alert } from '@mui/material';

export const ParametresDiagnosticsCIM10: React.FC = () => {
  return (
    <Box>
      <Alert severity="info">
        Cette section permettra de gérer la base CIM-10 : import, recherche, codes de diagnostic.
        (À implémenter)
      </Alert>
    </Box>
  );
};

