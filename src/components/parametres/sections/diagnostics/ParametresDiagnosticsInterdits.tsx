import React from 'react';
import { Box, Typography, Alert } from '@mui/material';

export const ParametresDiagnosticsInterdits: React.FC = () => {
  return (
    <Box>
      <Alert severity="info">
        Cette section permettra de gérer les diagnostics interdits/masqués.
        (À implémenter)
      </Alert>
    </Box>
  );
};

