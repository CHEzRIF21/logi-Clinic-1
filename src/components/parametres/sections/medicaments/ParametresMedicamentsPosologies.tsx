import React from 'react';
import { Box, Typography, Alert } from '@mui/material';

export const ParametresMedicamentsPosologies: React.FC = () => {
  return (
    <Box>
      <Alert severity="info">
        Cette section permettra de gérer les posologies standard (templates) pour chaque médicament.
        (À implémenter)
      </Alert>
    </Box>
  );
};

