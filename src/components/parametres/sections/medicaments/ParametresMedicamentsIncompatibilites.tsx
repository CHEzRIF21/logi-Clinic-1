import React from 'react';
import { Box, Typography, Alert } from '@mui/material';

export const ParametresMedicamentsIncompatibilites: React.FC = () => {
  return (
    <Box>
      <Alert severity="info">
        Cette section permettra de gérer les incompatibilités médicamenteuses.
        (À implémenter)
      </Alert>
    </Box>
  );
};

